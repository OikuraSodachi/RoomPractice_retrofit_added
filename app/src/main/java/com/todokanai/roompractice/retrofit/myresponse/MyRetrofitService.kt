package com.todokanai.roompractice.retrofit.myresponse

import retrofit2.Call
import retrofit2.http.GET
import retrofit2.http.Query

interface MyRetrofitService {
    /*
    @GET("")        // BASE_URL의 뒷부분
    fun getResponse(@Query("") nodeId: String ): Call<MyRetrofitRepository> // Call <Kotlin Data Class File to Json 으로 만든 클래스 MyRetrofitRepository
     */
}